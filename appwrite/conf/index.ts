import {
  url,
  projectId,
  databaseId,
  storageId,
  userCollectionId,
  postCollectionId,
  saveCollectionId,
} from '@/appwrite/env';

const appwriteConfig = {
  url,
  projectId,
  databaseId,
  storageId,
  userCollectionId,
  postCollectionId,
  saveCollectionId,
};

export default appwriteConfig;
